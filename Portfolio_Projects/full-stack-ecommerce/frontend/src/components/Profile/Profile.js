import React from "react";
// Create a form with the same inputs and state managment as the register form
// send an update request on sumbission
const Profile = () => {
  return <></>;
};

export default Profile;
